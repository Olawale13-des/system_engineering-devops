#!/usr/bin/env bash
# Lists the previous `chmod +x` invocation (the only non‑shell process) under the USER header

echo "USER"
ps -eo user,cmd --no-headers | grep "chmod +x" | head -n1 | awk '{print $1, $2, $3}'
