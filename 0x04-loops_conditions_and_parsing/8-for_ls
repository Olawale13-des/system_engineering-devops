#!/usr/bin/env bash
# This script lists the current directory content, displaying only the part after the first dash

for file in *; do
  if [[ -f "$file" && "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done
